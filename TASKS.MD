# TASKS

## Active

- [x] Rewrite Stage 2 ARC training around persistent prize-shaped sessions: replace reset-separated episode collection in `arc_public.py`, `goal_scientist_public.py`, and `scientist/train_arc.py` with session-based collectors, soft level-boundary resets, level-completion/session-win targets, and checkpoint selection by session outcomes instead of first positive reward or per-episode return.
- [x] Align the ARC runtime path with prize-session semantics: expose explicit `RESET`, keep the environment alive across `GAME_OVER`, preserve learned semantics across in-session level retries and completed-level boundaries, and stop treating first positive reward as terminal success.
- [x] Fix post-binder probe downstream valuation so stale movement hypotheses are state-conditioned, contradicted by no-effect repeats, and spotlight consumes hypothesis risk during scoring.
- [x] Integrate the action-level spotlight scientist into the live public scientist path, including delayed binder-probe credit, spotlight aliases, and checkpoint compatibility.
- [x] Tighten goal-scientist Stage 2 target quality so movement-only deltas stop reading as progress, zero-reward terminal endings become explicit setbacks, and Stage 2 trains on signed outcome targets instead of optimistic all-failure pseudo-progress.
- [x] Fix the goal-scientist public trainer so `learned` and `hybrid` are distinct collectors again, updates happen during the episode stream instead of only after full-epoch collection, and epoch seeds do not silently repeat unless explicitly requested.
- [x] Make the goal-scientist public ARC trainer observable and less wasteful under long offline runs by reusing a shared arcade and emitting per-episode collection progress.
- [x] Integrate the `arcdreamer_goal_scientist_update` patch set and validate the new goal-scientist Stage 2 training path.
- [x] Add a scientist offline-ARC repeated-episode trainer, run it on local `AR25`, and measure whether same-game world-model carryover helps.
- [x] Add a reproducible scientist-agent curriculum training path with checkpoint save/load, run a bounded pretrain, and re-evaluate local ARC `AR25`.
- [x] Integrate the GPT-5.4-pro scientist patch set from `GPT-PRO-PATCH-SET` into the live repo and validate the touched package, harness, and tests.
- [x] Move the generic runtime hypothesis controller into the full hybrid eval path instead of leaving it as sidecar research code.
- [x] Add explicit proof and exception objects to the runtime hypothesis loop and write them into episodic memory.
- [x] Add a first online representation-repair substrate: split/merge/rebind repair hypotheses now accumulate within the episode and suppress premature exploitation when the current object view looks wrong.
- [x] Add local per-episode model patches over action value, usefulness, policy prior, and uncertainty so the agent can edit bad expectations without waiting for full retraining.
- [x] Upgrade representation repair from diagnostics into actual online split/merge/relabel/rebind execution over the working state graph.
- [x] Replace heuristic objective scoring with normalized rival hypothesis posteriors and explicit action-conditioned likelihood comparisons.
- [x] Add option induction on top of proof objects so short reusable programs become first-class within-episode actions rather than just planner traces.
- [x] Replace geometry-only repair proposal gating with an online learned repair scorer that can generalize split/merge/rebind proposals across signatures within the episode.
- [x] Broaden posterior competition beyond single pairwise objective stories: family-level approach/contact/interact/activate/avoid theories now compete as executable hypotheses.
- [x] Add interface-aware first-contact experiment programs so the runtime controller tests control binding and follow-up action semantics from generic action-schema structure rather than ARC-shaped controller labels.
- [x] Replace stale pre-plan language carryover with chosen controller-plan language so episodic memory, diagnostics, and traces reflect the actual live hypothesis/plan that won arbitration.
- [x] Fix the online ARC evaluation harness so the real public slice can run end-to-end under one shared scorecard instead of failing with repeated scorecard-open conflicts.
- [x] Separate interface-only probe targets from reward-bearing objective targets so raw clickables stay probe candidates but stop blocking real goal pursuit until they earn semantic evidence.
- [x] Instrument synthetic holdout timing and remove structural eval waste: holdout logs now break out frontier/regression seconds, reuse one eval agent per holdout event, and support slower regression cadence without shrinking frontier capability.
- [x] Add optional async synthetic holdout evaluation on a second device: the trainer can now keep training on one GPU while a spawned worker loads epoch checkpoints on another GPU, runs the same holdout logic, and feeds results back into history and stage gating.
- [x] Promote the second device from sparse async holdout-only use into mirrored replay/dream training: when `--async-eval-device` points at a second CUDA device, the trainer now uses it for synchronized secondary optimization work every epoch and falls back to async holdout only when that device is not usable for real training.
- [x] Remove the collector-policy mismatch in Stage 1: `mixed` / `bootstrap` synthetic training now roll in with the current hybrid agent cloned from the latest weights instead of teaching only from the graph baseline plus oracle overlays.
- [x] Fix synthetic episode identity and reproducibility: explicit reset seeds now update the environment base seed, and dataset collection no longer advances constructor and reset seeds out of sync.
- [x] Fix Stage 2 ARC public collection hygiene: public-data collection now closes toolkit environments per episode, supports learned collector roll-ins, and exposes an explicit device flag instead of silently defaulting to whatever CUDA device is present.
- [x] Harden Stage 1 checkpoint continuation semantics: tracked training checkpoints now refuse silent weights-only init by default, and trainer startup logs now state whether a run is `scratch`, `resume`, or explicit `weights_only_init` together with the loaded stage/epoch context.
- [x] Remove positive reward-hack loops from hidden synthetic mechanics: `delayed_order_unlock`, `selector_sequence_unlock`, and selector local-match false progress no longer pay the agent for repeated failure/reset behavior, so holdout return tracks real task progress instead of decoy exploitation.
- [x] Remove dense teacher control from `mixed` warm starts: early Stage 1 now uses dense teacher supervision over learner-owned trajectories plus disagreement-triggered bootstrap takeover instead of forcing the first imitation epochs to be `100%` teacher-controlled.
- [x] Fix CLI/config drift for teacher-control and theory-objective defaults: `python -m arcagi.training.synthetic ...` now inherits the same lowered teacher fractions and explicit theory/diagnostic loss weights as `SyntheticTrainingConfig`, and parser regression coverage prevents silent reintroduction of stale defaults.
- [x] Add generic trajectory hindsight credit to Stage 1 collection/replay: each learner-owned episode now annotates discounted return, future progress mass, future setback mass, replay priority, and outcome-conditioned teacher disagreement weight instead of training purely from one-step transition labels.
- [x] Add an explicit long-horizon outcome head to the recurrent world model and route it into planner scoring/counterfactual replay so delayed consequences are represented directly rather than only through replay weighting.
- [x] Add explicit negative-effect runtime theories and immediate causal-control coupling: the theory DSL now distinguishes harmful-but-state-changing `setback` rules from neutral `state_change`/`no_effect`, contradiction/support/counterfactual theory events patch online action/context/family bias immediately, and stale within-episode bias decays instead of dominating after early mistakes.
- [x] Temper prior-driven control by fresh evidence rather than letting local priors snowball indefinitely: planner memory/induced/online-bias bonuses are now reliability-scaled by recent no-effect/cycle evidence, and the failed long-run checkpoint `fresh_noncontinuation_diag_hindsight_return.epoch_0021.pt` improved on hidden-modes holdout from `0.2` frontier success / `-0.833` avg return to `0.3` / `-0.642` under the same weights.
- [x] Fix flat joint-training supervision so compositional control/sequence events actually train the model: Stage 1 samples now use event-conditioned usefulness/policy targets instead of discarding `event`, synthetic observations expose generic public interface state (`selected` / progress / goal flags) through the normal observation channel, and flat mode now applies adaptive family sampling with a rehearsal floor instead of uniform family cycling.
- [ ] Stress-test the live repair workspace, induced options, and family-level posterior controller on longer synthetic runs and the real ARC public slice; the architecture gap is closed, but transfer/generalization is still not proven.
- [x] Record the generalized-reasoning pivot in [docs/PIVOT_GENERAL_REASONING.md](docs/PIVOT_GENERAL_REASONING.md) and treat it as the governing standard for future synthetic/training changes.
- [x] Record the allocentric spatial-workspace pivot in [docs/PIVOT_GENERAL_REASONING.md](docs/PIVOT_GENERAL_REASONING.md) and treat it as a required substrate for generalized planning under layout/interface shift.
- [x] Add the first agent-owned spatial belief workspace to the runtime state path: persistent visited/tested/effect/contradiction place memory now augments structured state with coarse spatial-belief summaries.
- [x] Sanitize agent-visible transition info on the learned/runtime path so hidden synthetic rule/event payloads remain available for offline diagnostics/training logs but do not leak into agent transitions.

- [x] Make ARC Prize eligibility a hard repo requirement in project tracking, not an implicit preference.
- [x] Review `AGENTS.MD` and extract hard constraints.
- [x] Confirm current repository contents and working assumptions.
- [x] Inspect current public ARC toolkit surface and pin the local API assumptions.
- [x] Create the package scaffold, docs, and reproducible project metadata.
- [x] Implement the environment contract and synthetic hidden-rule worlds.
- [x] Implement structured perception and explicit graph memory.
- [x] Implement recurrent world model, grounded language, and episodic memory.
- [x] Implement baseline agents and the full hybrid agent.
- [x] Add training and evaluation entrypoints.
- [x] Run the tests and a smoke experiment, then fix failures.
- [x] Tighten planner/training behavior after the initial smoke failures.
- [x] Remove the non-prize-eligible synthetic structured controller from the default execution path.
- [x] Preserve ARC-facing genericity while continuing toward toolkit validation.
- [x] Validate the official local ARC toolkit path on this machine and keep all non-generic synthetic machinery out of that path.
- [ ] Improve generic ARC-facing agent performance without reintroducing benchmark-specific hacks.
- [x] Add an allocentric spatial belief workspace that is agent-owned, persistent across steps, action-updated, and queried by planning/world-model code instead of relying on exact move-trace priors.
- [x] Integrate the spatial workspace as a hybrid substrate with the object graph and symbolic belief state, not as a raw duplicate of the visible grid.
- [x] Use the spatial workspace to track explored-vs-unexplored regions, tested interaction sites, return-to-place anchors, and contradiction/effect hotspots under partial observability and size/layout shift.
- [x] Replace remaining handcrafted ARC-facing controller behavior with generic within-episode rule induction and action-schema exploration.
- [ ] Ensure the runtime agent learns online from its own transitions while it plays, rather than acting as a frozen checkpointed policy.
- [x] Beat the synthetic benchmark with a live runtime-learning learned agent rather than the deleted handcrafted controller.
- [ ] Prove that any ARC-facing control logic is transferable to non-text games with the same observation/action structure, rather than benchmark-specific.
- [ ] Gate or replace brittle checkpoint priors when live empirical evidence disagrees during runtime.
- [x] Add explicit rule objects and a hypothesis manager instead of relying mainly on scalar action statistics.
- [ ] Add stronger counterfactual replay and local proof/disproof writes from failed transitions.
- [ ] Add explicit diagnostic action selection for rule disambiguation.
- [ ] Prototype a diffusion-inspired hypothesis-denoising loop for the runtime hypothesis-to-action-to-learning-to-proof pipeline.
- [x] Commit to retraining on a richer synthetic curriculum before further real-ARC validation.
- [ ] Expand the synthetic curriculum with selector modes, hidden state, delayed reward, compositional rules, misleading affordances, and non-monotonic objectives.
- [ ] Retrain the learned stack on the richer synthetic curriculum on GPU when available.
- [x] Run the first post-audit GPU retrain on the corrected pipeline and record the resulting checkpoint and metrics.
- [x] Instrument the Stage 1 trainer for manual long runs with interval console metrics, per-epoch snapshots, and interrupt-safe checkpointing.
- [x] Fix Stage 1 epoch sampling so each epoch gets fresh synthetic episodes instead of replaying the same seeded dataset within a curriculum stage.
- [x] Fix Stage 1 interval logging so alternating family schedules do not alias into a misleading single-family view at fixed log strides.
- [x] Replace the fixed staged synthetic schedule with held-out gated stage promotion plus replay mixing.
- [x] Instrument Stage 1 holdout evaluation with step-level causal traces, event breakdowns, action-family counts, failure signatures, and persisted holdout payloads.
- [ ] Tune the new held-out synthetic promotion thresholds and eval cadence now that a `25k`-episodes-per-epoch manual run can clear foundation and reach `hidden_modes`; gating still needs to stay informative without dominating wall-clock time.
- [x] Recover foundation size-9 held-out competence enough to clear staged promotion: the long manual run now advances into `hidden_modes` by epoch `4`, so foundation is no longer the active blocker.
- [x] Remove planner-side probe fallback logic and replace it with target-conditioned evidence: rule induction and online bias updates now score local observable object signatures so failed probes suppress only the targeted signature instead of smearing across the whole interaction family.
- [ ] Suppress residual no-effect churn after promotion: the epoch-4 `hidden_modes` windows still carry many `48`-step failures and negative-return episodes, so the controller is still wasting actions after foundation has already been cleared.
- [ ] Fix long-horizon hidden-mode control and sequence generalization: after promotion, `selector_unlock` and `delayed_order_unlock` are still only around `0.45` running success with frequent timeout failures.
- [x] Fix the trainer-level long-horizon credit gap that was exposed once dense teacher control was removed: the current scratch `2 x 128` run with the return-head world model now clears foundation honestly at epoch `0` with holdout `success_rate = 1.0`, `avg_return = 0.862`, and `teacher_step_fraction = 0.325`, then opens hidden-modes epoch `1` at `0.844` success by episode `32` and `0.859` by episode `64` under `teacher_step_fraction ~= 0.25`.
- [x] Verify that the generalized negative-evidence/runtime-theory fix changes training behavior, not just replayed eval: a fresh scratch `2 x 128` run with the current stack now reaches hidden-modes holdout `success_rate = 0.6`, `avg_return = -0.154`, and regression `success_rate = 1.0` while keeping `teacher_step_fraction ~= 0.258`, so the short-run hidden-mode baseline is materially better even though promotion still fails.
- [x] Remove generic-memory contamination from the clean learned path: planner memory retrieval now ignores generic language overlap, ignores default `goal_active=0` flag claims, and only retrieves positive recommendations from non-counterfactual evidence.
- [x] Add a generic spatial interaction prior so blind `interact_*` actions are penalized when no object is actually adjacent in that direction.
- [x] Reduce blind rule-induction optimism so unseen actions do not receive a large synthetic explore prior by default.
- [x] Ground Stage 1 language targets more strongly in observable sequence progress by deriving `collect ... then ...` and `need test ... after ...` tokens from visible inventory/object state rather than hidden rule ids.
- [x] Fix Stage 1 two-variant family aliasing: `order_collect` and `delayed_order_unlock` now cycle both order variants within each epoch instead of training on only one variant per epoch because of modulo aliasing.
- [x] Stop synthetic post-goal reward farming: redundant mechanism interactions after `goal_active=1` now return a penalty instead of continuing to pay positive subgoal reward.
- [x] Persist full holdout history in Stage 1 checkpoints so interrupted long runs preserve the entire eval trajectory, not just the last payload.
- [x] Preserve hidden symbolic state in the learned state path: `StructuredState` summary/transition vectors now encode generic symbolic inventory/flag tokens instead of collapsing them to binary `"1"` checks, and synthetic observations now surface `progress_index` / `selected_color` for perception.
- [ ] Fix the remaining post-promotion blocker after the clean-path repairs: the world-model/policy prior still overvalues unproductive control interactions in `hidden_modes`, so the promoted run remains only partially competent instead of consolidating into a stable solve policy.
- [ ] Improve flat all-families generalization on held-out `selector_sequence_unlock`: the current repaired flat short run preserves `switch_unlock` / `order_collect` at `1.0` held-out success and raises short-run selector-sequence collection performance, but held-out selector-sequence remains only `1/6` solved on the quick probe and still times out on most variants.
- [x] Fix the world-model training path so the ensemble uncertainty is not explicitly collapsed and the recurrent hidden state is actually trained across episode sequences.
- [x] Retrain foundation long enough after the root-cause fixes above: a `25k`-episodes-per-epoch manual run now clears the foundation gate and advances into `hidden_modes` by epoch `4`.
- [x] Retrain long enough on the repaired Stage 1 objective to verify that the interaction suppression, memory gating, and stronger gate regularization can recover foundation competence: this is now demonstrated by the epoch-4 promotion into `hidden_modes`.
- [ ] Replace the old "best repaired foundation checkpoint" story with the real promoted long-run artifact chain and index the exact stage-advance checkpoint and holdout payload in the repo.
- [ ] Explain and stop post-promotion instability: the run reaches `hidden_modes`, but late epoch-4 windows still swing between roughly `0.375` and `0.500` interval success with many timeout failures.
- [x] Complete the Stage 1 training-objective pivot in code: policy supervision is now event-agnostic by default, smooths over equivalence classes for moves/clicks, and `mixed` collection was downgraded to bounded oracle bootstrap instead of acting as a dominant teacher path.
- [x] Fix the Stage 1 bootstrap failure on the pivoted path: `mixed` collection now starts with a short imitation warm-start and decays into sparse bounded bootstrap instead of trying to cold-start from almost pure exploration.
- [x] Replace the fixed epoch-count bootstrap handoff with a competence-gated apprenticeship: the trainer now holds dense bootstrap until the policy clears consecutive train-success thresholds instead of withdrawing support on a blind epoch clock.
- [x] Lengthen the default Stage 1 bootstrap schedule and expose the live bootstrap state in epoch metrics/checkpoints so future runs show whether success is policy-owned or still scaffold-carried.
- [x] Replace the one-way competence-gated bootstrap release with a permanent teacher floor plus DAgger-style learner-state relabeling: Stage 1 now keeps a pure-teacher episode floor and writes teacher action labels on learner-visited states instead of relying on a one-shot release epoch.
- [x] Keep nonzero teacher support for the full run without reverting to oracle-dominated trajectories: the trainer now maintains a minority of pure teacher episodes plus learner roll-ins with teacher labels on visited states.
- [x] Make theory and diagnostic structure first-class training targets: the recurrent world model now predicts causal value, diagnostic value, and causal-effect class directly, and replay/dream optimize explicit theory plus diagnostic-language targets instead of treating runtime theory as an untrained sidecar.
- [x] Stop dense warm-start supervision from turning into teacher-owned control: dense mixed/bootstrap supervision now keeps episode ownership with the learner, disables pure teacher episodes during dense phases, and lowers the default control pressure to `teacher_episode_fraction=0.1` and `teacher_takeover_prob=0.25`.
- [ ] Recover iteration speed after the causal/theory objective expansion: a short flat GPU smoke now takes roughly `128.8s` collection + `35.0s` replay + `6.1s` dream for only `32` episodes, which is too slow for the repo’s fast-iteration requirement.
- [ ] Convert the new theory/ownership objectives into held-out compositional generalization: after the causal/ownership patch, a short flat smoke still showed `selector_sequence_unlock` at only `0.286` collection success with negative return, so the hard family remains unsolved.
- [x] Replace the toy auxiliary language head with a richer grounded predicate scratchpad: the vocabulary now spans belief/question/plan predicates, Stage 1 and Stage 2 both train a real plan channel, and planner/memory/runtime code now carry learned plan tokens instead of reducing language to short toy captions plus `("plan", action)` logs.
- [ ] Make teacher guidance dynamic rather than latched: even after promotion into `hidden_modes`, the long run is still using `teacher_step_fraction≈0.259` and `teacher_relabel_fraction≈0.622`, so support is still substantial and should rise again automatically on regression.
- [ ] Quantify post-promotion learner ownership under the permanent-teacher-floor trainer: clearing foundation is not enough if `hidden_modes` competence disappears once teacher density is reduced further.
- [ ] Reduce teacher episode dependence after promotion: the epoch-4 run still used `teacher_episode_fraction=0.2`, `teacher_takeover_prob=0.5`, and `5062` teacher episodes by the end of the epoch.
- [ ] Add a real Stage 1 dream/consolidation phase: the current stack has act-time lookahead and tiny counterfactual replay, but it still lacks anchored latent rehearsal over short imagined futures rooted in real memory.
- [ ] Keep dream replay constrained and reality-checked: dream rollouts should start from real anchors, follow short real or teacher-supported future action chains, and train against later observed outcomes instead of unconstrained self-generated fantasy.
- [ ] Verify that dream replay improves post-imitation stability: the immediate target is to stop the epoch-2 degradation after full imitation ends without reverting to oracle-owned trajectories.
- [x] Fix the synthetic grounded-language target misalignment: belief/question supervision now describes the current state, not the post-action next state, so the language head is no longer explicitly trained to say `goal active` before activation.
- [x] Remove planner-side grounded-language fallback synthesis from the learned eval path: decoded language is no longer replaced by hand-authored grounded beliefs/questions when the planner dislikes it.
- [x] Replace planner-side color/probe heuristics with generic target-conditioned online evidence over observable object signatures.
- [ ] Redesign the Stage 1 objective so post-audit retraining improves the cleaned `AR25`/public-slice baseline instead of regressing it.
- [ ] Prevent the retrained policy/language heads from collapsing back into high-click churn on hard selector/click tasks such as `DC22`.
- [x] Decide whether the current mixed Stage 1 policy target is poisoning the harder-family action prior and replace it if so.
- [ ] Run a longer post-promotion continuation from the epoch-4 promoted checkpoint and measure whether `hidden_modes` success can be pushed materially above `0.45` without losing foundation competence.
- [ ] Produce the same style of held-out diagnostic report for promoted `hidden_modes` checkpoints that foundation now has, including family breakdowns for `selector_unlock` and `delayed_order_unlock`.
- [ ] Revalidate on the chosen real online 5-game slice only after the richer synthetic retrain completes.
- [ ] Beat the public online 5-game slice with the clean learned eval path only; current measured state after controller removal and multiple planner/adaptation passes is still `0/5`.
- [ ] Convert the new real-ARC first-contact activity into reward-bearing semantics: the current online 5-game slice now completes end-to-end with `ar25` at `5` interaction steps, `cn04` emitting `objective_competition`, and `dc22` at `16` interaction steps, but solved count is still `0/5`.
- [ ] Stabilize the post-separation family tradeoff on the real 5-game slice: the valid `epoch_0004` checkpoint and the newer degraded `interrupt` checkpoint now emphasize different games (`dc22` vs `cn04`) under the same controller logic, which means reward-target calibration is still unstable across checkpoints.
- [ ] Do the real-ARC reward handoff and checkpoint-tradeoff fix through learned state/model targets, not new controller policy gates: the controller may expose generic probes and memory hooks, but reward-objective conversion must be learned by the recurrent model, language scratchpad, and policy/value heads.
- [x] Make runtime theory formation a first-class learned-path subsystem: the hybrid agent now carries an explicit per-episode causal theory manager that maintains competing compact action/target effect theories, normalizes their posterior competition, emits contradiction/support/counterfactual theory events into episodic memory, and steers planner bias through diagnostic conflict signals instead of new controller gates.
- [x] Make runtime beliefs explicit instead of leaving them as hidden activations: theory competition now materializes `RuleCandidate` objects with grounded preconditions, effects, scope, support, contradiction count, posterior/confidence, and attached diagnostic actions.
- [x] Make experimentation epistemic in the planner path: the theory manager now emits explicit `diagnostic_action_scores`, and the planner/search-root ranking consumes them as a first-class action-selection signal instead of relying only on novelty, entropy, or generic online bias.
- [x] Fix pre-retrain planner-state mismatch: imagined selector follow-ups and deeper rollouts now use updated imagined state proxies instead of the stale original structured state/action context.
- [x] Fix pre-retrain search objective mismatch: `_lookahead(...)` no longer rewards uncertainty directly.
- [x] Fix graph recency across persisted synthetic episodes: `StateGraph` now uses monotonic recency ticks instead of local episode `step_index`.
- [x] Fix ARC click-affordance generation so it does not prioritize the smallest components first or silently omit larger important objects behind the `max_candidates` cap.
- [x] Make state fingerprints less brittle by removing episode-local object-id dependence from relation fingerprints.
- [x] Adapt the grounded language channel during Stage 2 public ARC training instead of leaving it frozen and stale while encoder/world-model representations move.
- [x] Add explicit logging for missing/unexpected keys during compatibility checkpoint loads so future migrations do not silently mask broken weights.
- [x] Remove the old hard color ceiling from the encoder/state path by supporting arbitrary color ids through bucketing and explicit background-color handling.
- [ ] Fully restore parity on the old easy synthetic slice under the current runtime stack before trusting any broader synthetic or real-ARC measurements.
- [ ] Finish the remaining narrow-slice recovery from the current partial `5/9` `switch_unlock` result to stable near-complete recovery across all original variants.
- [x] Isolate all hand-authored runtime-controller logic from the learned eval path so it can exist only as explicit bootstrap/training instrumentation.
- [x] Remove heuristic question-token fallback from the learned eval path so action choice depends on learned outputs, graph statistics, memory, and online updates only.
- [x] Remove remaining hand-shaped controller target-ranking logic from the ARC-facing runtime path and replace it with evidence learned from within-episode transitions.
- [ ] Make the runtime controller defer to the learned planner when hypothesis evidence is weak instead of forcing low-evidence selector or target-probe behavior.
- [x] Couple the learned latent state and grounded scratchpad more tightly to the runtime hypothesis loop so the explicit controller is not carrying nearly all online reasoning by itself.
- [x] Make selector and click hypotheses consume latent follow-up move predictions so hidden control-state changes can affect objective discovery even when the visible grid barely changes.
- [ ] Add stronger proof/disproof for latent selector-mode promises by comparing predicted post-selector gains against realized mover/objective improvement.
- [x] Remove the planner-only control mistake and restore unified arbitration where explicit reasoning can seize action choice when confident.
- [x] Replace full-width latent search with cached root predictions, claim-guided branch pruning, and a hard per-turn world-model-call budget so deeper lookahead does not explode combinatorially.
- [ ] Measure whether bounded deeper search improves `AR25` and the online 5-game slice without regressing runtime or steps.
- [ ] Improve the clean learned eval path on real ARC without reintroducing controller interception:
  current blockers are weak objective discovery / completion on `AR25`, `BP35`, `CD82`, `CN04`, and `DC22` even after the audit fixes removed most of the previous interaction churn.
- [ ] Fix post-level transfer and objective reacquisition on real `AR25`. The newer `artifacts/spotlight_exec_curriculum_best.pkl` checkpoint can now complete level 1 under an uncapped offline harness run by about step `300`, but it then stalls with repeated resets and no further reward through at least step `750`.
- [ ] Add an uncapped real-ARC eval metric alongside the capped holdout path. Current capped evals can miss genuine first-level capability, as shown by `AR25` reaching `levels_completed = 1` only after the old `256`-step budget.
- [x] Replace hand-tuned spotlight action scoring with a learned executive scorer over explicit workspace state while keeping the minimal generic spotlight protocol.
- [x] Add synthetic scientist-session training that explicitly exercises hidden binding, fail/reset/retry loops, and cross-level carryover in one persistent session.
- [ ] Verify that the learned executive scorer improves synthetic binder-to-probe-to-goal behavior without adding ARC-shaped controller hacks.
- [x] Add an explicit learned habit policy for synthetic scientist training, trained from a synthetic oracle/teacher rather than self-bootstrap.
- [x] Convert spotlight arbitration into an explicit habit-versus-executive split where habit is the default and executive predicts override advantage.
- [x] Log and validate move-37-style low-prior overrides: chosen action low-rank under habit, high-rank under executive, later supported by progress or disambiguation.
- [ ] Measure whether the synthetic teacher-trained habit prior actually restores nontrivial stage-1 success and meaningful stage-2 carryover in real curriculum runs, not just unit-test behavior.
- [ ] Stabilize teacher-free stage-1 transfer under the new habit prior. The first long curriculum run reached a transient `simple_success_rate=0.25` at session 24, then regressed to `0.0` by session 32 while rich holdout remained dead.
- [ ] Make the executive produce real validated override events. In the first long teacher-habit run, both `best` and `latest` checkpoints still had `move37_candidates=0` and `move37_validated=0`.
- [ ] Change the training objective from immediate action quality to attempt-to-attempt improvement quality: actions and updates should get credit when the next retry is better than the previous one.
- [ ] Add explicit adaptation metrics to the scientist trainer and spotlight diagnostics so we can measure whether the agent is learning how to learn within a session, not just whether it gets reward.
- [x] Change the training objective from immediate action quality to attempt-to-attempt improvement quality: actions and updates should get credit when the next retry is better than the previous one.
- [x] Add explicit adaptation metrics to the scientist trainer and spotlight diagnostics so we can measure whether the agent is learning how to learn within a session, not just whether it gets reward.
- [ ] Convert the new adaptation signal into stable teacher-free holdout gains. The first medium run produced nonzero `simple_avg_attempt_improvement`, `rich_avg_attempt_improvement`, and even a few `move37` events, but holdout success and levels completed remained zero.
- [ ] Fix the scientist checkpoint ranking so attempt-improvement cannot dominate when solve metrics are still zero. The long `spotlight_exec_curriculum` run selected a `best` checkpoint on higher `rich_avg_attempt_improvement` despite `rich_session_win_rate=0.0`, `rich_avg_levels_completed=0.0`, and a much worse `rich_avg_return`.
- [ ] Separate teacher-shaped training metrics from autonomous competence in scientist-train reporting and model selection. Current `recent_*` metrics come from `_run_teacher_session(...)`, so nonzero training wins do not mean the teacher-free policy is actually competent.
- [ ] Tie adaptation credit to solve-boundary progress instead of generic better-failure deltas. The long curriculum run now produces heavy `habit` / `executive` / `adaptation` updates and nonzero `move37` events, but holdout `simple_success_rate` and `rich_avg_levels_completed` stay at `0.0`.
- [ ] Fix reset-policy collapse in synthetic rich-session holdout. The long curriculum run reported `rich_avg_reset_steps = 4.0` at every observed holdout snapshot, which suggests the retry policy has converged to a fixed cadence instead of a learned state-dependent recovery decision.
- [x] Add prize-eligible real-ARC action semantics to the adapter, including effective-grid extraction and parameterized interaction affordances.
- [x] Replace blind real-ARC graph exploration with observation-grounded motion/target planning that still falls back to generic graph novelty.
- [x] Write the technical report with measured results.
- [x] Create `CONTEXT.MD` and begin maintaining explicit project-state entries.

## Assumptions

- Python 3.10 is available locally.
- PyTorch is already present in the environment.
- ARC toolkit integration should be optional and degrade gracefully when `arc-agi` / `arcengine` are absent.
- A strong first milestone is a fully runnable synthetic stack plus the ARC adapter surface, even if official benchmark execution is gated on local SDK availability.
- Learned-model ARC runtime support is not yet proven because the toolkit action mapping still needs local validation.
- Official toolkit validation on this machine now uses an isolated Python `3.13` environment because current `arc-agi` releases require Python `3.12+`.
- If this system ever truly solves ARC-AGI-3 for the right reasons, its control loop should transfer to a broad class of non-text games with similar observation/action structure; treat that as an architecture filter, not a post-hoc claim.
- Runtime adaptation is mandatory: the agent must update beliefs, memory, action priors, and planning state online from its own transitions during play.
- The runtime learner should eventually infer action semantics, object roles, reward rules, hidden-state beliefs, and diagnostic action value online, not just update scalar action scores.
- The current synthetic milestone is now satisfied by a runtime object-hypothesis controller plus online `avoid` / `prefer` correction rather than the deleted handcrafted synthetic controller.
- Real ARC evaluation must now be treated as strict black-box interaction. Environment source files, hidden engine logic, and per-game tuning are forbidden as agent knowledge.
- Any explicit rule-object system is allowed to have engineered containers and update rules, but the learned rule contents must come from live within-episode interaction only.
- "Black-box" is necessary but not sufficient: ARC-facing runtime behavior should be legibly driven by generic online learning rather than by controller-score tuning that merely avoids forbidden inputs.

## ARC Prize Eligibility Rule

- Final ARC-facing code must remain eligible for ARC Prize submission.
- No runtime internet dependency is allowed in the submission path.
- No benchmark-specific hardcoding or synthetic-task semantics may leak into the generic ARC-facing path.
- Synthetic curriculum machinery is allowed only for Stage 1 training/evaluation and must be cleanly isolated.
- Submission-relevant code and methods must remain reproducible and open-source compatible with ARC Prize rules.
- ARC-facing planning/control changes should be rejected if they would not plausibly transfer to non-text games outside ARC with comparable observation/action structure.
- ARC-facing runtime behavior should be rejected if it depends on a frozen checkpoint alone and does not improve or adapt from live within-episode experience.
- ARC-facing runtime behavior should also be rejected if it cannot explain its current behavior in terms of revisable grounded hypotheses, rule support, and contradiction.
- ARC-facing runtime behavior should be rejected if it encodes game-specific semantics, level-specific priors, or behavior derived from inspecting benchmark environment files.
- Real-ARC retries must continue to use the official adapter as a black-box interaction surface only; success claims from this point forward should mention whether they were validated offline, online, or both.
- Internal model-based rollouts are allowed to grow only under explicit compute budgets, cached reuse, and generic claim-guided pruning; brute-force full-width depth increases are not acceptable on the target hardware.
- The learned eval path may construct the generic runtime hypothesis controller.
- The learned eval path must not construct or consult any synthetic-task-specific or benchmark-shaped runtime controller unless that controller is being used strictly as explicit training/bootstrap instrumentation outside the submission-relevant path.
- The learned eval path must not synthesize heuristic language/question tokens when the learned language head is silent; empty learned output is preferable to a hand-authored fallback.
